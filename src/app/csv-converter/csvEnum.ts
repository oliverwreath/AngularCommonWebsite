export enum OutputType {
  Array = 'array',
  Hash = 'hash',
  Minify = 'minify',
}
export enum Separator {
  Comma = ',',
  SemiColon = ';',
  Pipe = '|',
}
export enum LineBreak {
  Default = '\n'
}
